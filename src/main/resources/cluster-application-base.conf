
akka {
  loglevel = INFO
  logger.logger-startup-timeout = 30s

  actor {
    provider = cluster
    serialization-bindings {
      "com.luzene.actor.SerializationMarker" = jackson-json
    }
  }

  remote {
    artery {
      enabled = on
      transport = tcp
    }
  }

}

clustering {
  cluster.name = incidents
}
