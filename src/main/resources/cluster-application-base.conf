
akka {
  loglevel = INFO

  actor {
    provider = cluster
    serialization-bindings {
      "com.ibm.neteng.actor.SerializationMarker" = jackson-json
    }
  }

  remote {
    artery {
      enabled = on
      transport = tcp
    }
  }

}

clustering {
  cluster.name = incidents
}
